<Window x:Class="LCModManager.EnterTextDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LCModManager"
        WindowStartupLocation="CenterOwner"
        mc:Ignorable="d"
        Height="140" Width="200" ResizeMode="NoResize">
    <Grid>
        <StackPanel>
            <TextBlock x:Name="MessageTextBox" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            <TextBox x:Name="InputTextBox" Margin="30,10,30,10" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="10"  HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <Button Content="Enter" Click="EnterButton_Click" IsDefault="True"  Margin="10,0,10,0"  Width="40" Height="20" />
            <Button Content="Cancel" Click="CancelButton_Click" IsCancel="True" Margin="10,0,10,0" Width="60" Height="20" />
        </StackPanel>
    </Grid>
</Window>
